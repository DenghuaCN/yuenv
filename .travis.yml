os: windows
language: node_js
node_js:
- 10.13.0

cache:
  directories:
  - node_modules
 
install:
- npm install

before_script:


script:
- npm run build

after_script:
  - cd ./dist
  - git init
  - git config user.name "denghua"
  - git config user.email "denghua.fm@foxmail.com"
  - git add .
  - git commit -m "Travis CI Auto Builder"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master :deploy
  # - ssh your-name@xx.xx.xx.xx 'cd /your-path && git fetch --all && git reset --hard origin/deploy && git pull'

branches:
  only:
  - master